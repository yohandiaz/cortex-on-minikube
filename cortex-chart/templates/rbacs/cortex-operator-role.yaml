apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: {{ .Values.namespace }}
  name: {{ .Values.cortexOperatorRole.name }}
rules:
  {{ toYaml .Values.cortexOperatorRole.rules | nindent 2}}
